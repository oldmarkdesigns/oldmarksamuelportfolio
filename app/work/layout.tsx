import WorkImageLightbox from '@/components/WorkImageLightbox'

export default function WorkLayout({ children }: { children: React.ReactNode }) {
  return <WorkImageLightbox>{children}</WorkImageLightbox>
}

